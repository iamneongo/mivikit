# MIVIKIT

Website giới thiệu và tư vấn tủ bếp inox MIVIKIT.

## Chạy local

```bash
pnpm install
pnpm dev
```

Project chạy mặc định tại `http://localhost:3001` khi biến môi trường `PORT=3001` được thiết lập.
